# Core
opencv-python>=4.8.0
numpy>=1.24.0
scipy>=1.11.0

# Data storage
# sqlite3 is built into Python — no extra package needed

# Graph / routing
networkx>=3.1

# Configuration
PyYAML>=6.0

# Audio guidance (Phase 4)
# On Raspberry Pi OS Bookworm, espeak-ng is used directly via subprocess.
# pyttsx3 is a fallback for macOS/Windows only — do NOT install on RPi
# (pyttsx3's espeak driver is broken on Python 3.13+ with espeak-ng).
# Install espeak-ng on RPi instead: sudo apt install espeak-ng
pyttsx3>=2.90; sys_platform != "linux"
pygame>=2.5.0

# Arducam ToF SDK — install manually from source on RPi:
# https://github.com/ArduCAM/Arducam_tof_camera
# The pip package name on PyPI is: ArducamDepthCamera
# Uncomment when running on Raspberry Pi:
# ArducamDepthCamera

# Development / testing
pytest>=7.4.0
pytest-mock>=3.11.0
